# Security policy

The reference core has no network client and no automatic external action. Report vulnerabilities privately before public disclosure when exploitation could expose user-entered text or bypass an intervention invariant.

This software is not an operating-system sandbox. Platform deployments must isolate untrusted models and extensions, minimize data retention and independently test authorization boundaries.
